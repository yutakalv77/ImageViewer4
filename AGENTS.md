# Gemini CLI Instructions

## Git コミットについて
- **重要:** 本エージェントによる**GitHubへのコミットおよびプッシュは一切行わないでください**。
- 変更はファイルへの保存のみとし、コミット作業は常にユーザーが手動で行います。
- コミットメッセージの提案が必要な場合は、日本語で行ってください。

## 報告スタイル
- 作業完了後の `git diff` による差分表示は行わないでください。
- 作業完了後の変更ファイル一覧の表示は行わないでください。
- 「ありがとうございました」「手動でのコミットをお願いします」といった定型的な挨拶や確認のメッセージは不要です。
- 完了報告は、実施した内容の要約のみを簡潔に伝えてください。

## 実装後の必須レビュー・リファクタリング方針
機能追加や改修の実装後は、完了とする前に**必ず以下のレビュー観点に基づいたセルフレビューおよびリファクタリングを実施してください**：

1. **クラス・関数・コンポーネントの責務分離（単一責任の原則: SRP）**
   - 1つのコンポーネントや関数にUI表示、OS・ウィンドウ操作、副作用・イベント監視、データ処理などの複数責務が集中していないか確認し、必要に応じてコンポーネントやカスタムフックに分離する。
   - 共通パターン（クリック外検知、キーボードショートカット等）の重複を避け、再利用可能なフックやコンポーネントに抽出する。

2. **純粋関数の抽出とテスト容易性**
   - UIコンポーネント内にパス解析、データ変換、フォーマットなどのロジックが埋め込まれている場合、副作用のない純粋関数としてユーティリティ等に切り出し、単体テストを作成する。

3. **将来の改修に耐えうる拡張性と保守性**
   - 新規機能追加時に既存コードの全面改修を避けるため、疎結合な設計とし、ハードコードを避ける。

4. **品質検証（テストと型整合性）の必須実行**
   - 実装・リファクタリング完了後、必ず全単体テスト（`npm test`）およびTypeScript型チェック・プロダクションビルド（`npm run build`）を実行し、エラーやリグレッションが0件であることを確認してから完了とする。
