# Gemini CLI Instructions

## Git コミットについて
- **重要:** 本エージェントによる**GitHubへのコミットおよびプッシュは一切行わないでください**。
- 変更はファイルへの保存のみとし、コミット作業は常にユーザーが手動で行います。
- コミットメッセージの提案が必要な場合は、日本語で行ってください。

## 報告スタイル
- 作業完了後の `git diff` による差分表示は行わないでください。
- 作業完了後の変更ファイル一覧の表示は行わないでください。
- 「ありがとうございました」「手動でのコミットをお願いします」といった定型的な挨拶や確認のメッセージは不要です。
- 完了報告は、実施した内容の要約のみを簡潔に伝えてください。

## 実装時の問い合わせについて
- ソースコードの変更や実装にあたり、特別な理由（破壊的な仕様変更や重大な設計判断の分岐など）がない限り、**ユーザーへ事前の問い合わせや都度の確認は行わず、自律的に直接実装を進めてください**。

## 設計・コーディング方針（実装時に必ず意識すること）
実装にあたっては、後からのリファクタリングに頼るのではなく、**設計・実装の段階から必ず以下の観点を意識してコーディングを行ってください**：

1. **クラス・関数・コンポーネントの責務分離（単一責任の原則: SRP）**
   - **UIコンポーネントのスリム化:** UI表示、IPC通信（Tauri invoke等）、非同期状態管理、ダイアログ・ウィンドウ操作などの複数責務を1つのコンポーネントに混在させない。データ取得や副作用処理は**カスタムフック**に分離し、UIコンポーネントは描画とイベントバインドに専念させる。
   - **共通パターンの集約:** クリック外検知、キーボードショートカット、リサイズ等の共通処理は再利用可能なフックやコンポーネントに抽出する。

2. **フレームワーク非依存の純粋関数の抽出とテスト容易性**
   - **フロントエンド:** パス解析、データ変換、フォーマット等のロジックは副作用のない純粋関数としてユーティリティ等に切り出し、単体テストを作成する。
   - **バックエンド (Rust):** `AppHandle` などのフレームワーク固有のハンドルに依存させず、パス（`&Path`）やプリミティブ型のみを引数に取る純粋関数・ヘルパー関数を切り出し、直接単体テストを実行できるようにする。

3. **将来の改修に耐えうる拡張性と保守性**
   - 新規機能追加時に既存コードの全面改修を避けるため、疎結合な設計とし、ハードコードを避ける。
   - 他のコンポーネントや将来の機能からも再利用可能な粒度でフックやユーティリティを設計する。

## 実装後の必須レビュー・リファクタリング方針
機能追加や改修の実装後は、完了とする前に**必ず上記の設計・コーディング方針に基づいたセルフレビューおよびリファクタリングを実施してください**：

1. 実装段階で責務分離（SRP）や純粋関数の抽出が徹底されているか再確認し、不足があればリファクタリングする。
2. 品質検証（テストと型整合性）の必須実行:
   - 全単体テスト（`npm test`、およびRust変更時は `cargo test`）を実行し、エラーが0件であることを確認する。
   - TypeScript型チェック・プロダクションビルド（`npm run build`）を実行し、エラーやリグレッションが0件であることを確認してから完了とする。

